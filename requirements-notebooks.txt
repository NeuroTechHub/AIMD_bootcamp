# Shared Python dependencies for the optional Jupyter notebooks.
#
# The browser HTML modules still run without Python. This file is for local
# notebook setup and for validation runs.
numpy
matplotlib
scipy
scikit-image
opencv-contrib-python
torch
# torchvision + einops kept: M2's gaze_workshop pip-installs both and they
# are transitive runtime deps of deepgaze_pytorch even when no notebook
# code explicitly imports them.
torchvision
einops
ipywidgets
dynaphos
ultralytics
# (timm pruned 2026-05 — no module imports it, no transitive consumer.)
Pillow
nbformat
nbclient
git+https://github.com/matthias-k/DeepGaze.git
